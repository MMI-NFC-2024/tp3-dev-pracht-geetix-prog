export interface Penguins {
    species: string
    island: string
    culmen_length_mm?: number
    culmen_depth_mm?: number
    flipper_length_mm?: number
    body_mass_g?: number
    sex?: string
}